<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:gravity="center">
        <ImageView android:id="@+id/metro_bg"
            android:layout_width ="match_parent"
            android:layout_height="match_parent"
            android:padding="0dp"
	    android:scaleType="fitXY"
            android:visibility="visible" />
        <LinearLayout
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:gravity="center"
        android:padding="15dp"
        android:orientation="vertical">

        <ImageView android:id="@+id/metro_item_logo"
            android:layout_width ="@dimen/metro_item_logo_size"
            android:layout_height="@dimen/metro_item_logo_size"
            android:padding="5dp"
            android:visibility="visible" />

	    <TextView android:id="@+id/metro_item_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/metro_item_title_size"
            android:textColor="@color/common_text_color"
            android:singleLine="true"
            android:ellipsize="end"
            android:layout_gravity="left"
            />
		
    	<TextView android:id="@+id/metro_item_summary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/metro_item_summary_size"
            android:textColor="@color/text_color_white_80"
            android:singleLine="true"
            android:ellipsize="end"
            android:layout_gravity="left"
            />
        </LinearLayout>
</FrameLayout>